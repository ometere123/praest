# Implementation Status

Status vocabulary:

- **IMPLEMENTED** — source exists for the frozen capability.
- **STATIC VERIFIED** — repository/static syntax checks have run successfully.
- **DEPENDENCY-RESOLVED VERIFIED** — real toolchain, real `node_modules`/crates/pip packages: typecheck, build, unit tests and/or the relevant linter actually ran and passed against this exact source.
- **NATIVE PROOF REQUIRED** — implementation exists but the required native compiler/toolchain was unavailable in the current session's environment.
- **LIVE PROOF REQUIRED** — requires user credentials, provider accounts, funded testnet keys or deployed addresses.

This session (2026-09-02/03) did what the previous assembly environment explicitly could not: ran a real `npm install`, a full dependency-resolved `typecheck`/`test`/`build` across all 16 workspace packages, `forge build`/`forge test` for the EVM contracts, `gltest`/`genvm-lint` for the GenLayer Intelligent Contracts, and pushed to a real `github.com/ometere123/praest` with GitHub Actions actually executing on Linux runners. That surfaced and fixed a number of real bugs that had never been caught because nothing had ever actually run before (see the git log for full detail on each).

**2026-09-04 update**: A second pass migrated Resend→Brevo, made Stripe fully optional (`STRIPE_ENABLED`), deployed all 9 GenLayer Intelligent Contracts to StudioNet, and wired four of them (`AgreementRegistry`, `EvidenceAssessor`, `SettlementEntitlement`, `LiabilityResolver`) that had addresses but no code path reading them - see commits `f5d259a`..`b1878f5`. It also fixed several real P0 trust/security gaps found in an external review: an x402 self-attested-payment endpoint, TLSNotary self-verification via the ordinary (non-webhook) endpoint, a WorkOS bootstrap tenant-escalation path, a GenLayer `finalize()` gated on a nonexistent transaction status, premature-refund gaps in both the EVM and Solana escrows, and missing semantic idempotency on the Hyperlane gateway's dispatch. Full detail in the commit messages.

| Subsystem | Source status | Verification status |
| --- | --- | --- |
| Product / Control / Developer / Explorer route surfaces | IMPLEMENTED | STATIC VERIFIED route manifest (165 URLs). Frontend redesigned this session (design system, component library, marketing site) and visually verified in a real browser (light/dark theme, desktop/mobile) for the public/unauthenticated surfaces; authenticated Product/Control/Developer/Explorer views require real WorkOS sign-in to render — LIVE PROOF REQUIRED for those |
| Next.js auth/API relay | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED (typecheck + `next build`). Three real config bugs found and fixed this session: `.env.example` named the wrong WorkOS redirect-URI variable, the AuthKit middleware's route matcher was silently blocking `/public` static assets, and Next 16 needs `moduleResolution: bundler` (never previously exercised). Real WorkOS sign-in still LIVE PROOF REQUIRED |
| WorkOS → Privy custom JWT + embedded wallets | IMPLEMENTED | 2026-09-04: fixed a real tenant-escalation bug in `POST /v1/bootstrap` - it accepted `body.workosOrganizationId` as a fallback whenever the caller's WorkOS JWT had no `org_id` claim, letting any authenticated caller become `owner` (`permissions:["*"]`) of an arbitrary existing organization by supplying its id. Organization identity now comes exclusively from the verified JWT claim. LIVE PROOF REQUIRED |
| NestJS/Fastify API | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED (typecheck + build). Real bugs fixed: missing `Inject` import, a nonexistent `TooManyRequestsException` export, `noUncheckedIndexedAccess` violations, and a real TLS-verification-disabled bug in the Postgres connection (see security fix commit) |
| PostgreSQL/Drizzle domain model + migration | IMPLEMENTED | typecheck/build pass; migration/live Postgres proof required |
| Upstash limits/cache | IMPLEMENTED | LIVE PROOF REQUIRED |
| Temporal workflows (self-hosted, Temporal Cloud kept optional) | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED (typecheck + build). Real bug fixed: `@temporalio/worker` has no `Connection` export — the worker-side connection type is `NativeConnection`. Self-hosted Railway deployment attempted 2026-09-04: `temporalio/auto-setup:1.24.2` reaches schema setup successfully but the server process OOMs on startup (`/etc/temporal/auto-setup.sh: fork: Out of memory`) at this Railway service's current memory allocation — LIVE PROOF BLOCKED pending a memory-limit increase on the `temporal-server` service (Railway dashboard → Settings → Resources; not adjustable via the available Railway MCP tools). Project: `railway.com/project/72ef4f3d-2a24-4c51-872a-57959cffc0bd` |
| ClickHouse measurements | IMPLEMENTED | init/live ingestion proof required |
| R2 evidence storage | IMPLEMENTED | LIVE PROOF REQUIRED |
| Regional probes (Globalping default, `native` provider available, AWS Lambda retired) | IMPLEMENTED | typecheck/build/test pass. 2026-09-04: fixed a real credential-leak bug - a monitor target carrying `authHeaders` (a credentialed/private check) was being forwarded verbatim to Globalping, a public third-party probe network; this is now a hard invariant failure (`fail("...cannot be used for authenticated targets...")`), not just a configuration convention. Test suite updated/extended (16/16 passing). live Globalping measurement proof required |
| monitoring DNS/TCP/TLS/TTFB/assertions/SSRF controls | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED; live network matrix required |
| evidence bundles/provenance/hashes | IMPLEMENTED | local/API proof required |
| TLSNotary integration | IMPLEMENTED adapter (config-only; no real MPC-TLS prover/verifier built yet) | 2026-09-04: hardened so only the `TLSNOTARY_WEBHOOK_TOKEN`-authenticated webhook can mark evidence `tlsnotary_verified` — the ordinary `/v1/tlsnotary/proofs` endpoint can no longer self-attest verification (`b.verified` was previously trusted from any authenticated caller). Real verifier service still LIVE PROOF REQUIRED / not started |
| GenLayer Intelligent Contracts | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED: `genvm-lint check` passes for all 9 Intelligent Contracts. **2026-09-04: all 9 deployed to StudioNet** (`deployments/studionet.json` has real tx hashes + resolved addresses). 4 of 9 (`AgreementRegistry`, `EvidenceAssessor`, `SettlementEntitlement`, `LiabilityResolver`) were deployed in a prior session but had no code path reading their addresses — now wired: terms are committed to `AgreementRegistry` at agreement activation and verified before adjudication; evidence bundles are submitted to `EvidenceAssessor` on lock; finalized remedies are recorded to `SettlementEntitlement` and independently re-verified before settlement is built; `LiabilityResolver` routing was simply missing from `resolverAddress()` and is now wired. Live StudioNet adjudication (submit → decided → appeal → finalize) transaction proof still required |
| GenLayer decided/appeal/finality lifecycle | IMPLEMENTED | **2026-09-04: fixed a real bug** — `finalize()` was gated on `txStatus === "READY_TO_FINALIZE"`, a transaction status that does not exist in GenLayer's protocol, so `finalize()` was silently never being called; it's now always attempted once the appeal deadline has passed, tolerating an already-progressed transaction. StudioNet LIVE PROOF REQUIRED |
| deterministic settlement engine | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED (typecheck/build); DB integration proof required |
| Studio DecisionOutbox relay | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED (typecheck/build); StudioNet + zkSync LIVE PROOF REQUIRED |
| EVM gateway/receiver/escrow | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED: `forge build` + `forge test -vvv` pass, now 31/31 (2 suites) after this session's additions. Two real bugs fixed previously (brace-less-if, stack-too-deep). **2026-09-04, 2 more real fixes**: (1) `PraestEscrow.refundUnused()` let the payer withdraw the full remaining balance any time nothing had settled yet - including mid-active-agreement/mid-open-dispute; added a 7-day lock (`DEFAULT_REFUND_LOCK_SECONDS`) plus a `SETTLER_ROLE`-gated `settlerRefund()` for legitimate early cancellation. (2) `StudioDecisionGateway.dispatchInstruction()` had no idempotency - a retried bridge cycle could dispatch a duplicate Hyperlane message/fee for the same instruction; it now takes an explicit `instructionId`, tracks `dispatchedInstructionMessage`, and returns the existing `messageId` (with a full refund) on retry. 17 new Foundry tests added, all passing |
| Hyperlane real-ISM design / route config | IMPLEMENTED | `scripts/hyperlane-smoke.ts` typechecks/builds; live route/ISM proof required |
| Solana Testnet recipient/escrow | IMPLEMENTED | A real type-mismatch bug (`H256` vs `[u8;32]` in the trusted-sender check) was found and fixed via GitHub Actions' Linux runner, which — unlike this session's Windows machine — has a working linker and reached actual compilation. NATIVE PROOF REQUIRED locally: this session's Windows environment has Rust/Cargo installed but lacks MSVC Build Tools (needs admin elevation not available) and the Solana/Agave CLI install also requires admin elevation; `cargo build-sbf` and live deployment remain undone. **2026-09-04**: applied the same premature-refund fix as the EVM escrow (`lock_until` field + time lock on `refund()`, plus a Config-owner-gated `OwnerRefund` instruction for legitimate early release) and documented (did not fix) the existing `MAX_PROCESSED=64` replay-protection cap as a known structural limit needing a per-instruction-PDA redesign. **Not compiler-verified this session** - this machine's Rust toolchain has no MSVC linker at all (only Git's unrelated `link.exe` on PATH), so even `cargo check` fails on unrelated third-party build scripts before reaching this crate; verified by manual review only. Confirmed no TS caller encodes this program's instruction enum today, so the new enum variant's discriminant shift is safe pre-deployment. Run `cargo check --features no-entrypoint` in CI or on a machine with a working toolchain before deploying |
| EVM/Solana non-custodial escrow funding + independent confirmation | IMPLEMENTED | `EscrowFundingAction.tsx` typechecks/builds; wallet/testnet LIVE PROOF REQUIRED |
| destination reconciliation/receipts | IMPLEMENTED | cross-chain LIVE PROOF REQUIRED |
| x402 V2 facilitator lifecycle | IMPLEMENTED (verify/settle bookkeeping only) | 2026-09-04: removed the `receipt()` endpoint/route, which let an authenticated caller mark any x402 request "paid" by posting arbitrary JSON - paid status now only ever comes from a verified facilitator settlement. **Known gap, not yet built**: nothing in the codebase actually challenges a request with an HTTP 402 + payment requirements (the "seller" half of the protocol) - this needs a product decision (which endpoint/resource gets paywalled, at what price) before it can be implemented. facilitator/payment LIVE PROOF REQUIRED |
| agents/tasks/execution/liability data paths | IMPLEMENTED | end-to-end live proof required |
| event/prediction resolution | IMPLEMENTED | StudioNet/live proof required |
| Internet Court interoperability adapter | IMPLEMENTED | provider LIVE PROOF REQUIRED |
| billing/meter events/webhooks (optional, humans free by default) | IMPLEMENTED | 2026-09-04: added `STRIPE_ENABLED` (default `false`) - checkout/portal/usage/webhook now fail cleanly with `BillingDisabledError` instead of requiring any Stripe credential to boot; `GET /v1/billing/config` lets the frontend hide Subscribe/Manage Billing cleanly while disabled. Human access to the rest of PRAEST was already never gated by billing state (RBAC has no billing check). Stripe LIVE PROOF REQUIRED for the enabled path |
| Brevo/webhook notifications | IMPLEMENTED | 2026-09-04: migrated from Resend to `@getbrevo/brevo` - same optional no-op-if-unset behavior, now validates `BREVO_FROM_EMAIL` is configured before attempting to send. 3 unit tests added, passing. LIVE PROOF REQUIRED (no sender/domain verified in Brevo yet) |
| TypeScript SDK | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED (typecheck + build) |
| Python SDK | IMPLEMENTED | local import/API test required |
| CLI | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED (typecheck + build; required the turbo task-graph fix below to resolve `@praest/sdk`'s types) |
| MCP v2 server | IMPLEMENTED | DEPENDENCY-RESOLVED VERIFIED (typecheck + build). Real bug fixed: `serveStdio()` returns a synchronous handle, not a Promise |
| CI/security workflows | IMPLEMENTED | Pushed to `github.com/ometere123/praest`; GitHub Actions now actually execute (previously never proven). `CI` and the `semgrep`/`gitleaks`/`codeql` jobs of `Security` are green. `Security`'s `trivy` job is a known, deliberate red: real HIGH/CRITICAL vulnerabilities in `@privy-io/react-auth`'s and `@hyperlane-xyz/relayer`'s own transitive dependencies, not fixable without a breaking downgrade — see git log |

## Environment limitations (this session, Windows)

- `npm install`, `forge build`/`forge test`, and `gltest`/`genvm-lint` all work reliably now.
- Rust/Cargo installed via rustup, but linking requires MSVC Build Tools, which requires admin elevation this session did not have. `cargo check`/`cargo build-sbf` for `contracts/solana` are blocked locally; GitHub Actions' Linux runner does not have this limitation and is the actual source of the Solana bug fix above.
- Solana/Agave CLI install also requires admin elevation; not installed.
- Provider secrets and funded testnet wallets are intentionally not available.
- 2026-09-04: Railway CLI/MCP access became available this session (`railway.com/project/72ef4f3d-2a24-4c51-872a-57959cffc0bd`, project `praest` created). Self-hosted Temporal deployment is blocked on the service's memory allocation (OOM at startup), not a config/code issue - see the Temporal row above.

These are verification limitations, not deferred product scope. `scripts/doctor.mjs` exposes them explicitly - as of 2026-09-04 it also reports per-feature-group env-variable presence (Temporal, Globalping, Stripe, Brevo, x402, TLSNotary, Internet Court, GenLayer wired-vs-unwired) without ever printing a value, and `docs/ENVIRONMENT.md` is the full authoritative env variable matrix.
