import Console from "@/components/Console";import {notFound} from "next/navigation";import {routes} from "@praest/config";import {withAuth} from "@workos-inc/authkit-nextjs";
const publicPaths=new Set(['/about','/how-it-works','/pricing','/developers','/docs','/security','/status']);const known=new Set(Object.values(routes as any).flat());
export default async function Page({params}:{params:Promise<{segments?:string[]}>}){const {segments=[]}=await params;const path='/'+segments.join('/');if(publicPaths.has(path))return <main className="hero"><div className="hero-inner"><div className="eyebrow">PRAEST</div><h1>{path.slice(1).replaceAll('-',' ')}</h1><p>Accountability infrastructure for digital services and autonomous commerce.</p></div></main>;await withAuth({ensureSignedIn:true});const normalized=path.replace(/\/[0-9a-f]{8}-[0-9a-f-]{27,}/ig,'/[id]');const match=[...known].some((r:any)=>{const pattern='^'+String(r).replace(/\[[^\]]+\]/g,'[^/]+')+'$';return new RegExp(pattern).test(path)});if(!match&&!path.startsWith('/app')&&!path.startsWith('/control')&&!path.startsWith('/developer')&&!path.startsWith('/explorer'))notFound();return <Console path={path}/>}
