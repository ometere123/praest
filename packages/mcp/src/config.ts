export const MCP_NAME = "praest-direct" as const;
export const MCP_INSTRUCTIONS = "PRAEST is direct-contract on Studio-dev (61997). ACCEPTED is provisional; require FINALIZED plus FINISHED_WITH_RETURN before claiming success." as const;
